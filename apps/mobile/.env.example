# 실기기(Expo Go)에서 API 서버에 접속할 URL.
# localhost 불가 — PC의 LAN IP를 사용한다 (ipconfig → IPv4 주소).
# 예: EXPO_PUBLIC_API_URL=http://192.168.1.10:8000/api/v1
# 또는 Railway 배포 URL: EXPO_PUBLIC_API_URL=https://<your-app>.railway.app/api/v1
EXPO_PUBLIC_API_URL=http://192.168.x.x:8000/api/v1
