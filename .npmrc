# Expo/Metro는 pnpm의 심볼릭 node_modules와 충돌이 잦다.
# 데모 안정성(NFR8) 우선 → hoisted 링커로 평탄화하여 단일 React 해석 보장(AR2/G5).
node-linker=hoisted

# React Native 관련 패키지는 항상 호이스트(중복 React/RN 인스턴스 방지)
public-hoist-pattern[]=*react-native*
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*expo*
